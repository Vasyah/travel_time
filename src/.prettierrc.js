const config = {
  semi: false, // Добавлять точку с запятой в конце выражений (true по умолчанию)
  singleQuote: true, // Использовать одинарные кавычки вместо двойных (false = двойные кавычки)
  tabWidth: 2, // Размер табуляции (2 пробела — стандарт для многих проектов)
  useTabs: false, // Использовать табуляцию вместо пробелов (false = пробелы)
  trailingComma: 'es5', // Добавлять запятые в конце многострочных объектов/массивов (es5 = только в массивах/объектах)
  bracketSpacing: true, // Добавлять пробелы внутри фигурных скобок (true = "{ привет }")
  arrowParens: 'avoid', // Всегда использовать скобки для стрелочных функций (always = "(x) => {}")
  endOfLine: 'lf', // Конец строки (lf = Unix-style, auto = зависит от ОС)
  jsxSingleQuote: false, // Использовать одинарные кавычки в JSX (false = двойные кавычки)
  quoteProps: 'as-needed', // Когда добавлять кавычки к свойствам объектов (as-needed = только если необходимо)
  printWidth: 80, // Максимальная ширина строки (80 символов — стандарт, но можно увеличить до 100 или 120)
  proseWrap: 'always', // Как обрабатывать длинные строки в Markdown (preserve = сохранять исходное форматирование)
  jsxBracketSameLine: false, // Закрывающая скобка на новой строке
}

module.exports = config;
