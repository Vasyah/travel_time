# ExportHotels Feature

Фича для экспорта списка отелей в формате, удобном для отправки в Telegram/WhatsApp.

## Описание

Позволяет пользователям быстро экспортировать найденные отели в виде форматированного текста с автоматическим копированием в буфер обмена.

## Компоненты

### ExportHotelsButton

Кнопка-иконка для открытия модального окна экспорта.

**Особенности:**

- Отображается рядом с расширенными фильтрами в форме поиска
- Автоматически отключается, если нет отелей для экспорта
- Имеет всплывающую подсказку (tooltip)
- Использует иконку FileText из lucide-react

**Использование:**

```tsx
import { ExportHotelsButton } from '@/features/ExportHotels';

<ExportHotelsButton />;
```

### ExportHotelsModal

Модальное окно с таблицей отелей и функцией копирования.

**Особенности:**

- Автоматически копирует список при открытии
- Показывает компактную таблицу с номерами отелей и ссылками на Telegram
- Предпросмотр форматированного текста
- Кнопка для повторного копирования
- Обработка случая, когда нет отелей

**Props:**

```typescript
interface ExportHotelsModalProps {
    isOpen: boolean; // Состояние открытия модалки
    onClose: () => void; // Callback для закрытия
    hotels: HotelForRoom[]; // Массив отелей для экспорта
}
```

**Использование:**

```tsx
import { ExportHotelsModal } from '@/features/ExportHotels';

<ExportHotelsModal isOpen={isModalOpen} onClose={() => setIsModalOpen(false)} hotels={hotels} />;
```

## Утилиты

### formatHotelsForMessenger

Форматирует список отелей для отправки в мессенджеры с использованием Markdown разметки.

**Формат вывода:**

```
📋 *Список отелей*

1. *Название отеля*
   [Telegram](https://t.me/...)

2. *Другой отель*
   [Telegram](https://t.me/...)
```

### formatHotelsAsPlainText

Форматирует список отелей в виде простого текста без Markdown.

**Формат вывода:**

```
📋 Список отелей

1. Название отеля
   Telegram: https://t.me/...

2. Другой отель
   Telegram: https://t.me/...
```

### copyToClipboard

Универсальная функция для копирования текста в буфер обмена с fallback для старых браузеров.

**Использование:**

```typescript
const success = await copyToClipboard(text);
if (success) {
    console.log('Скопировано!');
}
```

## Интеграция

Фича интегрирована в форму поиска (`SearchForm`) и использует store `$hotelsFilter` для получения списка отелей.

### Зависимости

- `effector-react` - для подключения к store
- `@/shared/models/hotels` - store с отфильтрованными отелями
- `lucide-react` - иконки
- `@/components/ui/*` - UI компоненты (Dialog, Button, Tooltip)
- `@/shared/ui/Toast/Toast` - уведомления

## Структура файлов

```
features/ExportHotels/
├── ui/
│   ├── ExportHotelsButton.tsx   # Кнопка для открытия модалки
│   └── ExportHotelsModal.tsx    # Модальное окно с таблицей
├── lib/
│   └── formatHotels.ts          # Утилиты форматирования
├── index.ts                      # Экспорты
└── README.md                     # Документация
```

## Примеры использования

### В форме поиска

```tsx
import { ExportHotelsButton } from '@/features/ExportHotels';

<div className="flex gap-2">
    <AdvancedFilters />
    <ExportHotelsButton />
    <Button onClick={handleSearch}>Найти</Button>
</div>;
```

### Программное копирование

```typescript
import { formatHotelsAsPlainText, copyToClipboard } from '@/features/ExportHotels';

const hotels = [
    { id: '1', title: 'Отель 1', telegram_url: 'https://t.me/hotel1' },
    { id: '2', title: 'Отель 2', telegram_url: 'https://t.me/hotel2' },
];

const text = formatHotelsAsPlainText(hotels);
await copyToClipboard(text);
```

## UX особенности

1. **Автоматическое копирование** - при открытии модалки текст сразу копируется в буфер обмена
2. **Визуальная обратная связь** - кнопка "Скопировано" с галочкой на 2 секунды
3. **Предпросмотр** - пользователь видит, как будет выглядеть текст
4. **Таблица** - удобный визуальный формат для просмотра
5. **Ссылки** - кликабельные ссылки на Telegram в таблице
6. **Отключение кнопки** - если нет отелей, кнопка неактивна

## Будущие улучшения

- [ ] Добавить экспорт в Excel/CSV
- [ ] Добавить фильтрацию отелей в модалке
- [ ] Добавить сортировку
- [ ] Добавить возможность выбора формата (с Markdown / без)
- [ ] Добавить экспорт дополнительных полей (адрес, телефон и т.д.)
- [ ] Добавить шаринг напрямую в Telegram через API
