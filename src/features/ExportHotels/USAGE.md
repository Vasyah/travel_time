# Руководство по использованию ExportHotels

## Быстрый старт

### Для пользователей

1. **Выполните поиск отелей** в форме поиска
2. **Нажмите на иконку 📄** рядом с расширенными фильтрами
3. **Список автоматически скопируется** в буфер обмена
4. **Вставьте текст** в Telegram или WhatsApp - форматирование сохранится!

### Для разработчиков

#### Добавление кнопки экспорта

```tsx
import { ExportHotelsButton } from '@/features/ExportHotels';

// Просто добавьте компонент - он сам получит данные из store
<ExportHotelsButton />;
```

#### Использование модального окна отдельно

```tsx
import { ExportHotelsModal } from '@/features/ExportHotels';
import { useState } from 'react';

function MyComponent() {
    const [isOpen, setIsOpen] = useState(false);
    const hotels = [...]; // ваши отели

    return (
        <>
            <button onClick={() => setIsOpen(true)}>
                Экспорт
            </button>

            <ExportHotelsModal
                isOpen={isOpen}
                onClose={() => setIsOpen(false)}
                hotels={hotels}
            />
        </>
    );
}
```

#### Программное копирование без UI

```tsx
import { formatHotelsAsPlainText, copyToClipboard } from '@/features/ExportHotels';

async function exportHotels(hotels) {
    const text = formatHotelsAsPlainText(hotels);
    const success = await copyToClipboard(text);

    if (success) {
        alert('Скопировано!');
    }
}
```

## Примеры форматирования

### Пример 1: Несколько отелей

**Входные данные:**

```javascript
const hotels = [
    {
        id: '1',
        title: 'Гранд Отель',
        telegram_url: 'https://t.me/grandhotel',
    },
    {
        id: '2',
        title: 'Морской Бриз',
        telegram_url: 'https://t.me/seabreeze',
    },
];
```

**Результат копирования:**

```
📋 Список отелей

1. Гранд Отель
   Telegram: https://t.me/grandhotel

2. Морской Бриз
   Telegram: https://t.me/seabreeze
```

### Пример 2: Отель без Telegram

**Входные данные:**

```javascript
const hotels = [
    {
        id: '1',
        title: 'Отель без Telegram',
        telegram_url: null,
    },
];
```

**Результат:**

```
📋 Список отелей

1. Отель без Telegram
   Telegram: Нет ссылки
```

### Пример 3: Пустой список

**Входные данные:**

```javascript
const hotels = [];
```

**Результат:**

```
Нет доступных отелей для экспорта
```

## UI компоненты

### ExportHotelsButton

#### Состояния

**Активна (есть отели):**

- Иконка: 📄
- Цвет: серый (outline)
- Tooltip: "Экспорт списка отелей"
- Действие: открывает модалку

**Неактивна (нет отелей):**

- Иконка: 📄 (серая, затемненная)
- Кнопка disabled
- Tooltip: "Нет отелей для экспорта"
- Действие: ничего

### ExportHotelsModal

#### Секции модального окна

1. **Заголовок**

    - Название: "Экспорт списка отелей"
    - Описание: информация об автоматическом копировании

2. **Таблица**

    - Столбцы: №, Название отеля, Telegram
    - Скроллбар при большом количестве отелей
    - Кликабельные ссылки на Telegram

3. **Предпросмотр**

    - Показывает финальный текст
    - Моноширинный шрифт
    - Серый фон

4. **Кнопки**
    - "Закрыть" - закрывает модалку
    - "Копировать снова" - повторное копирование
    - "Скопировано" (с галочкой) - показывается 2 секунды

## Интеграция с мессенджерами

### Telegram

Форматирование отлично работает в:

- Личных чатах
- Групповых чатах
- Каналах

**Совет:** Используйте режим предпросмотра Telegram для проверки ссылок.

### WhatsApp

Форматирование работает в:

- Личных чатах
- Групповых чатах

**Примечание:** WhatsApp не поддерживает полноценный Markdown, но текст остается читаемым.

## Troubleshooting

### Проблема: Кнопка неактивна

**Причина:** Нет отелей в store `$hotelsFilter`

**Решение:**

1. Убедитесь, что выполнен поиск
2. Проверьте, что найдены отели
3. Проверьте консоль на наличие ошибок

### Проблема: Не копируется

**Причина:** Браузер блокирует доступ к clipboard

**Решение:**

1. Проверьте разрешения браузера
2. Убедитесь, что страница открыта по HTTPS (или localhost)
3. Попробуйте другой браузер

### Проблема: Неправильное форматирование

**Причина:** Отсутствуют данные в объектах отелей

**Решение:**

1. Проверьте структуру объектов отелей
2. Убедитесь, что поля `title` и `telegram_url` заполнены
3. Проверьте типы данных

## API Reference

### formatHotelsAsPlainText

```typescript
function formatHotelsAsPlainText(hotels: HotelForRoom[]): string;
```

Форматирует массив отелей в текстовый список.

**Параметры:**

- `hotels` - массив объектов отелей типа `HotelForRoom[]`

**Возвращает:**

- `string` - отформатированный текст

**Пример:**

```typescript
const text = formatHotelsAsPlainText([
    { id: '1', title: 'Отель', telegram_url: 'https://t.me/hotel' },
]);
console.log(text);
```

### copyToClipboard

```typescript
async function copyToClipboard(text: string): Promise<boolean>;
```

Копирует текст в буфер обмена.

**Параметры:**

- `text` - строка для копирования

**Возвращает:**

- `Promise<boolean>` - успешность операции

**Пример:**

```typescript
const success = await copyToClipboard('Текст для копирования');
if (!success) {
    console.error('Ошибка копирования');
}
```

## Best Practices

1. **Всегда показывайте feedback пользователю** - используйте toast уведомления
2. **Проверяйте наличие отелей** перед показом кнопки
3. **Обрабатывайте ошибки** копирования
4. **Тестируйте в разных браузерах** - особенно Safari
5. **Учитывайте mobile** - на мобильных устройствах clipboard API работает иначе

## Changelog

### v1.0.0 (2024-10-13)

- ✨ Первый релиз
- ✨ Добавлена кнопка экспорта
- ✨ Добавлено модальное окно
- ✨ Автоматическое копирование
- ✨ Поддержка Telegram/WhatsApp форматирования
